<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/public/stylesheet/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body class="">
    <section class="container mx-auto p-10 bg-[#ECEFF1] min-h-screen">
        <!-- Form Section -->
        <div class="bg-[#CFD8DC] p-6 rounded-md shadow-md">
            <form action="/create" method="post" class="flex flex-col gap-4">
                <input type="text" name="title" placeholder="Title goes here..."
                    class="bg-[#B0BEC5] text-[#263238] block w-full rounded-md outline-none px-3 py-2 border border-[#90A4AE]">
                <textarea placeholder="Description goes here..." type="text" name="details"
                    class="bg-[#B0BEC5] text-[#263238] block w-full rounded-md outline-none px-3 py-2 border border-[#90A4AE]"></textarea>
                <input name="additional" placeholder="Additional info..."
                    class="bg-[#B0BEC5] text-[#263238] block w-full rounded-md outline-none px-3 py-2 border border-[#90A4AE]">
                <input type="submit" value="create "
                    class="bg-[#455A64] w-[10%]   hover:bg-[#37474F] px-4 py-2 text-white rounded-md mt-4"></input>
            </form>
        </div>

        <div class="flex flex-wrap gap-6 mt-10">
            <% if(files.length> 0) { %>
                <% files.forEach(function(file) { %>
                    <div class="w-72 p-6 bg-[#CFD8DC] rounded-md shadow-md">
                        <h1 class="text-xl text-[#263238] tracking-wider">
                            <%= file.replace('.txt', '' ).substring(0, 20) + '...' %>
                        </h1>
                        <a href="/files/<%= file %>"
                            class="text-[#37474F] border border-[#546E7A] bg-[#B0BEC5] px-3 py-1 inline-block mt-4 rounded-md">Read
                            More</a>
                    </div>
                    <% })%>
                        <% } else { %>
                            <h3 class="text-[#455A64] mt-5">No task found</h3>
                            <% } %>
        </div>
    </section>

</body>

</html>